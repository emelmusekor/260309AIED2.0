# Todo
- Verify in GitHub Pages that the repository uses either GitHub Actions or the main/master `/docs` source so the renamed docs/ bundle is actually the live publish root.
- Run a manual workbook page audit in a real browser for both elementary and secondary bundles to catch any other missing overlay boxes like the page 7 secondary gaps.
- Verify the new workbook reader flow in a browser, especially page switching, focus retention, and mobile ergonomics after long sessions.
- Verify worksheet email sharing UI, responsive menu reset, modal focus behavior, keyboard focus rings, topmost-Escape behavior, and older-browser fallback behavior in a real browser once a browser/runtime harness is available.
- Add regression coverage for search/filter interaction when workbook sections hide nonmatching pages inside the new reader.
